# 简历网站（Vite + Vue 3）

这是一个简洁现代的个人简历网站示例，基于 Vite + Vue 3。

快速开始：

```bash
npm install
npm run dev
# 访问 http://localhost:5173
```

说明：
- 组件在 `src/components` 下
- 全局样式在 `src/assets/styles.css`
- 可直接使用页面右上角的“下载 / 打印”按钮生成 PDF

接下来的建议改进项：
- 使用真实内容替换占位文本
- 添加国际化（i18n）支持
- 添加导出为 PDF 的服务端生成（可选）
